# Передача второму участнику

Документ для того, кто подключается к работе над решением. Читается за пять минут, после чего сервис запускается локально и можно править код.

## Что это

Сервис маскирования персональных данных для хакатона. Находит в тексте типы персональных данных из технического задания, маскирует их перед отправкой в языковую модель и восстанавливает в ответе.

Читать начинать с одного места: [SYSTEM-DESIGN.md](SYSTEM-DESIGN.md), это обзор решения целиком на десять минут, из него ссылки уходят вглубь. Оглавление всех документов с пометкой кому что читать лежит в [README](../README.md). Дальше по надобности: устройство кода в [ARCHITECTURE.md](ARCHITECTURE.md), запуск и адреса стенда в [QUICKSTART.md](QUICKSTART.md).

## Чего нет в репозитории и что нужно получить отдельно

Секреты и данные намеренно не выкладываются. Запросите их у первого участника любым защищённым способом, но не через сам репозиторий и не через задачи в нём.

| Что | Зачем | Где взять |
|---|---|---|
| Файл `.env` | ключ шифрования хранилища, ключи доступа систем, токен языковой модели | у первого участника; образец полей в `.env.example` |
| Доступ по SSH к стенду | посмотреть работающий сервис, снять нагрузку | у первого участника, ключ добавляется в облаке |
| Наборы данных в каталоге `corpus` | измерение качества | порождаются заново командой `make corpus`, загружаются скриптами в `tools` |

Без файла `.env` сервис запустится и предупредит об этом в журнале: создаст временный ключ шифрования и выключит системы, у которых не задан хеш ключа доступа. Анонимный профиль проверяющей системы ключа не требует, поэтому контракт `POST /process` работает сразу. Не хватит только режима прокси к языковой модели и профилей с ключами.

## Запуск за одну минуту

```bash
cp .env.example .env        # можно оставить поля пустыми для первого запуска
make build && make run      # сервис на порту 8080

# в другом окне
make verify                 # проверка контракта: маска, повтор, обратное преобразование
open http://localhost:8080/ui   # страница проверки в браузере
```

## Состояние репозитория: читать до того, как что-то делать

В ветке `main` лежит исходное состояние. Вся работа ночного прогона улучшений
собрана в ветке `night-integration`: шестнадцать веток `night/*`, слитых в одну
и проверенных целиком. Она проходит ворота и ждёт слияния.

```bash
git log --oneline main..night-integration --merges   # что в ней
```

Ветки `night/*` по отдельности трогать не нужно: их конфликты уже разрешены в
сборке, и слияние по одной приведёт к повторному разбору тех же мест.

## Как убедиться, что ничего не сломано

Одна команда вместо трёх. Ворота это семнадцать проверок от формата исходников
до поиска персональных данных в журнале и показателях.

```bash
make gate           # полный прогон, несколько минут
make gate-quick     # без набора данных и без схем, быстрее
bash scripts/gate.sh --list   # состав проверок
```

Ноль означает, что правку можно вливать. Любое другое число означает, что
нельзя, и в выводе написано почему. **Правку без открытых ворот не вливаем** —
это единственное жёсткое правило в проекте.

Две вещи про ворота, которые стоит знать заранее.

Файлы `scripts/baseline.json` и `scripts/perf-baseline.json` это базовые линии
качества и скорости. Меняет их только человек и только осознанно: линейка,
которую двигает тот же, кто правит код, ничего не измеряет. Ворота отдельно
проверяют, что базовая линия не изменена вместе с кодом.

Отрицательные срезы `neg_*` обязаны оставаться строго нулевыми, допуска у них
нет. Правка, которая поднимает один тип и ломает отрицательный срез, считается
неверной и откатывается, а не дорабатывается.

Отдельные проверки, если нужна только часть:

```bash
make test        # все тесты, включая детектор гонок
make lint        # формат и статический анализ
make quality     # качество на своём наборе данных, выводит таблицу по типам
```

Порядок работы над правилами обнаружения такой: сначала посмотреть на промахи, потом править, потом снова замерить.

```bash
go run ./cmd/gen -out corpus/dataset.jsonl -n 4000 -seed 42   # набор, если его нет
go run ./cmd/score -dataset corpus/dataset.jsonl              # таблица по типам за секунды
go run ./cmd/score -dataset corpus/dataset.jsonl -type ADDRESS -examples 20  # конкретные промахи
```

Главное правило: после правки обязательно смотреть на отрицательные категории с именами `neg_*` — и смотреть в колонку **«ложных»**, а не «изменено».

Здесь раньше стояло, что ложных срабатываний у этих категорий ноль. Это было неверно, и неверно по забавной причине: колонка «изменено» считается как доля изменённого на число размеченных фрагментов, а в отрицательной категории размеченных фрагментов нет — значит ноль по построению, а не по качеству. Ворота сравнивали с нулём величину, которая нулём была всегда. Проверка самих ворот это пропустила, потому что подставляла подложное значение в ту же бессмысленную колонку.

Как на самом деле на 23.09.2026: ненулевые ложные срабатывания у 18 категорий из 45, худшие — `neg_account_number` 31.98%, `neg_student_ticket` 29.64%, `neg_support_phone` 24.47%, `neg_company_inn` 23.18%, `neg_company_like_surname` 20.96%, `neg_historical_figure` 12.93%. Эти числа записаны в `scripts/baseline.json` в разделе `ложные` как снимок состояния, а не как одобренная норма: ворота ловят их рост. Снижение — отдельная работа.

Правка, которая поднимает один тип и увеличивает долю ложных в отрицательной категории, считается неверной.

## Где что лежит

| Каталог | Что внутри |
|---|---|
| `internal/pii` | токенизатор, детекторы по типам, контекстные правила, разрешение пересечений, словари |
| `internal/mask` | виды маскирования |
| `internal/store` | хранилище соответствий: память процесса и общее поверх Redis |
| `internal/engine` | связывает детекторы, правила системы и маскирование, режет длинные тексты на куски |
| `internal/api` | контракт проверки, прокси к модели, ручка разбора, страница проверки |
| `internal/config` | настройки и их применение без перезапуска |
| `internal/metrics` | показатели в формате Prometheus |
| `internal/logging` | журнал: стандарт полей, уровни, второй рубеж защиты от утечки значений, глушение повторов, журнал аудита, ротация |
| `cmd/gen`, `cmd/corpus` | порождение и сборка наборов данных |
| `cmd/score` | быстрый замер качества без сети |
| `cmd/sonar` | имитатор проверяющей системы с подсчётом качества |
| `cmd/loadgen` | чистый генератор нагрузки для поиска потолка |
| `docs` | документация, точка входа `docs/SYSTEM-DESIGN.md`, оглавление в `README.md` |
| `deploy/terraform` | описание стенда, разворачивается OpenTofu: сеть, машины, балансировщик `balancer.tf`, группа копий `scaling.tf`, правила роста `autoscale.tf` |
| `deploy/grafana` | панель наблюдения |
| `deploy/logging` | установка сбора журнала на машину: journald, ротация, архив аудита, вариант с Loki |
| `deploy/k6` | независимая проверка нагрузкой инструментом k6 |
| `scripts` | сценарии для жюри, проверка контракта, нагрузочные прогоны |

## Как делить работу, чтобы не мешать друг другу

Детекторы лежат отдельными файлами по типам данных, и правки в разных файлах не конфликтуют. Общие для всех места, которые лучше менять по договорённости: `internal/pii/helpers.go`, `internal/pii/doc.go`, `internal/pii/registry.go`, `internal/config/config.go` и `configs/config.yaml`.

Пакет `internal/logging` тоже общий: он подключён к `internal/api` и задаёт стандарт полей, поэтому правка формата записи задевает и разбор журнала, и сценарий жюри по критерию 6. Меняется по договорённости, состав полей зафиксирован в [LOGGING.md](LOGGING.md).

Документы делятся по темам, и у каждого один владелец на время правки. Разделение такое:

| Область | Файлы кода и настроек | Документ |
|---|---|---|
| Обнаружение и маскирование | `internal/pii`, `internal/mask`, `internal/engine` | [ARCHITECTURE.md](ARCHITECTURE.md), [MASK-FORMAT.md](MASK-FORMAT.md) |
| Контракт и точки входа | `internal/api`, `internal/config`, `configs/config.yaml` | [ARCHITECTURE.md](ARCHITECTURE.md), [../README.md](../README.md) |
| Журнал | `internal/logging`, `deploy/logging` | [LOGGING.md](LOGGING.md) |
| Показатели и панель | `internal/metrics`, `deploy/grafana`, `deploy/prometheus.yml` | [OBSERVABILITY.md](OBSERVABILITY.md) |
| Балансировка | `deploy/terraform/balancer.tf` | [BALANCING.md](BALANCING.md) |
| Рост под нагрузкой | `deploy/terraform/scaling.tf`, `deploy/terraform/autoscale.tf` | [SCALING.md](SCALING.md) |
| Замеры и наборы данных | `cmd/gen`, `cmd/corpus`, `cmd/score`, `cmd/sonar`, `cmd/loadgen` | [../BENCHMARKS.md](../BENCHMARKS.md), [LOAD-TESTING.md](LOAD-TESTING.md), [CORPUS-SOURCES.md](CORPUS-SOURCES.md) |

Правило простое: правишь код в области, правишь и её документ в том же заходе. Число, которое появилось в документе, должно быть замеренным либо помеченным словом оценка с показанным выводом.

Замер качества после каждой правки обязателен: без него легко починить один тип и незаметно сломать другой.

## Что нельзя класть в репозиторий

Ключи и токены, содержимое каталога `corpus`, файлы состояния инфраструктуры, скомпилированные файлы. Всё это уже перечислено в `.gitignore`, но проверять стоит: `make dist-check` собирает архив и сообщает, если в него попало лишнее.
