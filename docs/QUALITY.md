# Качество: полная картина

Документ описывает, что решение находит, чего не находит и где ставит маску
лишнюю. Все числа сняты прогоном `cmd/score` на наборе из 10 000 размеченных
записей 23.09.2026 и воспроизводятся двумя командами:

```
go run ./cmd/gen   -out corpus/dataset.jsonl -n 8000 -seed 42
go run ./cmd/score -dataset corpus/dataset.jsonl -split
```

Набор пересобирается из генератора при каждом прогоне ворот, а его отпечаток
лежит в `scripts/corpus.sha256` рядом с базовой линией. Это не формальность:
набор однажды отстал от генератора, линия осталась снятой на старом наборе, и
сравнение потеряло смысл, ничем себя не выдав.

> **ЧИСЛА ЖДУТ ПЕРЕСЧЁТА ПОСЛЕ СЛИЯНИЯ ВЕТОК.** Таблицы ниже сняты на одной
> из двух слитых веток. Вторая ветка доводила свои типы и свои детекторы:
> якорь паспорта до служебного слова и слипшиеся коды подразделения, ФИО без
> опоры на регистр, гражданство, публичные лица, латинские имена, место
> рождения, адрес по частям. Её правок этот прогон не видел, поэтому числа
> сдвинутся — причём по обоим спискам сразу: ветки чинили разные типы.
>
> Что вторая ветка намерила на себе и чего нет в таблицах ниже:
>
> | Тип | Вторая ветка | В таблицах ниже |
> |---|---|---|
> | PASSPORT | 0.9687 | 0.9362 |
> | DEPT_CODE | 0.9544 | 0.9518 |
>
> Разбор промахов и рассуждения в теле документа от пересчёта не зависят: они
> про природу ошибок, а не про их число. Пересчитать документ целиком стоит
> после выката на стенд — тогда же пересчитываются стендовые числа в
> [`BENCHMARKS.md`](../BENCHMARKS.md).

## Что означает число

Основная мера — **доля изменённого**: какая часть размеченного фрагмента
оказалась под маской. Единица означает, что фрагмент закрыт целиком; ноль —
что он остался в тексте открытым. Мера намеренно строгая: наполовину
закрытый паспорт защитой не является.

Ложные срабатывания измеряются **отдельной колонкой** и только на
отрицательных категориях — текстах, где персональных данных нет вовсе, но
есть что-то похожее по форме. Смешивать их с долей изменённого нельзя: у
отрицательной категории размеченных фрагментов нет, и доля изменённого равна
нулю по построению, а не по качеству.

## Семнадцать типов задания

Среднее 0.9576, минимум 0.9265.

| Тип | Доля изменённого |
|---|---|
| PIN | 0.9829 |
| CVV | 0.9827 |
| EMAIL | 0.9786 |
| CARDHOLDER | 0.9705 |
| ISSUE_DATE | 0.9626 |
| DOB | 0.9617 |
| INN | 0.9610 |
| DRIVER_LICENSE | 0.9610 |
| PHONE | 0.9607 |
| CARD | 0.9586 |
| ISSUER | 0.9567 |
| ADDRESS | 0.9556 |
| DEPT_CODE | 0.9518 |
| FIO | 0.9413 |
| PASSPORT | 0.9362 |
| CITIZENSHIP | 0.9301 |
| BIRTH_PLACE | 0.9265 |

## Одиннадцать типов сверх задания

Задание требует возможности расширения списка. Эти типы добавлены тем же
способом, каким добавляется любой новый: константа типа, детектор с якорями и
формой, категории в генераторе — включая отрицательные.

Среднее 0.9106.

| Тип | Доля изменённого |
|---|---|
| MILITARY_ID — военный билет | 0.9696 |
| BIRTH_CERT — свидетельство о рождении | 0.9468 |
| RESIDENCE_PERMIT — вид на жительство | 0.9439 |
| SNILS | 0.9433 |
| FOREIGN_PASSPORT — загранпаспорт | 0.9342 |
| POSTCODE — почтовый индекс | 0.9110 |
| OMS — полис ОМС | 0.8899 |
| VIN | 0.8840 |
| IP_ADDRESS — адрес в сети | 0.8824 |
| ACCOUNT — номер счёта | 0.8584 |
| PLATE — госномер автомобиля | 0.8527 |

## Ложные срабатывания

Сорок пять отрицательных категорий: реквизиты организаций, телефоны горячих
линий, номера заказов, названия фирм от фамилий, исторические лица, служебные
адреса в сети. Ненулевая доля ложных у четырнадцати, сумма долей 49.3%.

| Категория | Доля ложных |
|---|---|
| neg_company_inn | 17.04% |
| neg_student_ticket | 5.93% |
| neg_support_phone | 4.43% |
| neg_track_number | 3.57% |
| neg_account_number | 3.20% |
| neg_historical_figure | 3.15% |
| neg_imei | 2.90% |
| neg_vin | 2.36% |
| neg_medical_card | 1.73% |
| neg_oms | 1.41% |
| neg_company_like_surname | 1.25% |
| neg_insurance_policy | 0.94% |
| neg_ip | 0.91% |
| neg_money | 0.51% |

Утром 23.09 сумма была 165.6%. Снижение дали семь правок: бесплатные номера
800, 803 и 804 принадлежат организации; голая форма паспорта рядом со словом
«ИНН» паспортом не является; явное название другого документа отменяет
паспорт даже при якоре; ИНН, за которым идёт КПП, принадлежит юридическому
лицу; номер дома в слитной записи это одна-три цифры; фамилия после правовой
формы в кавычках — название фирмы; исторические правители добавлены в словарь
известных людей.

**ИНН организации остаётся замаскированным намеренно.** Набор размечает
десятизначный ИНН и как персональные данные, и как не-персональные в разных
категориях. Между утечкой ИНН человека и лишней маской на ИНН фирмы выбор
сделан в пользу «не утечь».

## Где решение слабее всего

**Место рождения, 0.9265.** Место пишут свободным текстом, и граница значения
размыта: «д. Заречье, Брянская область, Россия» — три части, любая из которых
может оказаться последней. Пропуски сосредоточены на записях, где после
значения идёт служебный хвост без знака препинания.

**Гражданство, 0.9301.** Название страны само по себе персональными данными
не является, поэтому нужен якорь. Пропуски там, где гражданство указано в
таблице без подписи столбца.

**Паспорт, 0.9362.** Остаток — записи, где серия и номер разнесены по разным
строкам таблицы.

**Текст со строчной буквы.** В этом прогоне среднее по типам задания падает
с 0.9576 до 0.9273, и почти весь провал даёт один тип: `FIO` 0.9413 → 0.6280.
Имя со строчной буквы неотличимо от обычного слова без словаря, а словарь
имён конечен. Следом идут `BIRTH_PLACE` 0.8498 и `CARDHOLDER` 0.8871.
Провал по `FIO` закрыт на второй ветке, и её правка вошла в слияние: разрыв
сократился с 0.30 до 0.05. Подробный замер — в разделе ниже.

**Строка выгрузки, 0.6169.** Самый слабый сценарий. В строке через табуляцию
значения стоят голыми, без подписей: `09/09/2018 г. 2898007147 20.4.1961
vasilev1966@gmail.com 575193 OK`. Типы, которым нужен якорь, здесь его не
находят. Сама структура строки — признак, и его решение пока не использует.

## Замер на тексте в нижнем регистре

В ТЗ есть прямое требование «Идентификация не зависит от регистра». Разрыв
между обычным текстом и текстом, приведённым к нижнему регистру, — прямая
мера выполнения этого требования. Замер: `-lower` (текст приводится к
нижнему регистру до маскирования), пресет `full`.

Строки `FIO`, `CARDHOLDER`, `BIRTH_PLACE` и `ISSUER` сняты на второй ветке,
уже после доводки регистронезависимости. Остальные строки несут числа,
снятые до обеих веток, поэтому колонка «Обычный» у них расходится с
таблицей семнадцати типов выше: `CITIZENSHIP` здесь 0.6194 против 0.9301
там. На нулевой разрыв у форматных типов это не влияет. Пересчитаются
вместе с остальным документом.

| Тип | Обычный | Нижний регистр | Разрыв |
|---|---|---|---|
| FIO | 0.9409 | 0.8888 | **−0.052** |
| CARDHOLDER | 0.9738 | 0.9296 | −0.044 |
| BIRTH_PLACE | 0.9296 | 0.8953 | −0.034 |
| CITIZENSHIP | 0.6194 | 0.5918 | −0.028 |
| ISSUER | 0.9582 | 0.9507 | −0.008 |
| ADDRESS | 0.9603 | 0.9409 | −0.019 |
| POSTCODE | 0.8527 | 0.8523 | −0.000 |
| PASSPORT | 0.7558 | 0.7558 | 0.000 |
| PIN | 0.7421 | 0.7421 | 0.000 |
| CVV | 0.7721 | 0.7721 | 0.000 |
| EMAIL | 0.9799 | 0.9799 | 0.000 |

Итог по наборам: `dataset` 0.8656 → 0.7978 (−0.068), `synthetic` 0.8619 →
0.7999 (−0.062), `ru_pii_66k` 0.9830 → 0.9301 (−0.053).

**Главная находка: FIO терял 0.30 на нижнем регистре** (0.93 → 0.63). Это было
прямым нарушением требования независимости от регистра: детектор ФИО опирался
на заглавные буквы как на якорь имени. После доводки разрыв сократился до
0.05 (0.9409 → 0.8888): имена, фамилии, отчества и инициалы опознаются и в
тексте, где всё строчными. CARDHOLDER и BIRTH_PLACE тоже проседают меньше
(0.03–0.04). Форматные типы (PASSPORT, PIN, CVV, EMAIL, POSTCODE) от регистра
не зависят — там разрыв нулевой.

## Виды маскирования

Замер на общем наборе, доля изменённого:

| Вид | Значение |
|---|---|
| `token` — плейсхолдер `[PASSPORT_1]` | 0.9959 |
| `initials` — инициалы | 0.9952 |
| `full` — звёздочки | 0.9496 |
| `synthetic` — подстановка правдоподобного значения | 0.9496 |
| `partial` — видны последние знаки | 0.5693 |

Низкое число у `partial` — не дефект, а его устройство: вид намеренно
оставляет часть значения видимой, и мера считает это неполным закрытием. Для
обмена с языковой моделью годятся `token` и `full`; `partial` предназначен для
показа человеку, который уже имеет право видеть данные.

## Что гарантируют ворота

`bash scripts/gate.sh` — семнадцать проверок одной командой. Для качества это
значит:

- ни один срез не проседает больше чем на 0.005 от записанной линии;
- доля ложных в отрицательных категориях не растёт;
- набор пересобирается заново и сверяется по отпечатку: сдвиг линейки
  закрывает ворота с прямым указанием снять линию осознанно;
- персональные данные не попадают в журнал и в показатели;
- скорость горячего пути не проседает больше чем на четверть.

Сами ворота проверяются отдельно: `bash scripts/gate-selfcheck.sh` подсовывает
каждой проверке заведомую поломку и требует, чтобы она покраснела. Одиннадцать
проверок. Это не избыточность: проверка ложных срабатываний однажды сравнивала
величину, нулевую по построению, и не могла сработать никогда.
